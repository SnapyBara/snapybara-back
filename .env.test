# Test Environment Variables
NODE_ENV=test

# MongoDB Test Configuration
MONGODB_URI=mongodb://localhost:27017/snapybara-test

# Supabase Test Configuration (use test project or mock)
SUPABASE_URL=https://test.supabase.co
SUPABASE_ANON_KEY=test-anon-key
SUPABASE_SERVICE_ROLE_KEY=test-service-key

# JWT Configuration
JWT_SECRET=test-jwt-secret-key
SUPABASE_JWT_SECRET=test-jwt-secret-key
JWT_EXPIRATION=1h

# API Configuration
API_PORT=3001
API_PREFIX=api
API_VERSION=v1

# Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=100

# Redis (optional for tests - will be skipped if not configured)
# REDIS_HOST=localhost
# REDIS_PORT=6379

# External APIs (mock for tests)
GOOGLE_MAPS_API_KEY=test-google-api-key
GOOGLE_PLACES_API_KEY=test-google-places-key

# Email Configuration (mock for tests)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test
SMTP_PASS=test
EMAIL_FROM=test@example.com

# Storage Configuration
STORAGE_TYPE=local
STORAGE_LOCAL_PATH=./test-uploads

# Sentry (disabled for tests)
SENTRY_DSN=

# Firebase (mock for tests)
FIREBASE_PROJECT_ID=test-project
FIREBASE_CLIENT_EMAIL=test@test.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\ntest-key\n-----END PRIVATE KEY-----"
