// FICHIER OBSOLÈTE - Tests pour méthodes d'authentification via backend
// L'authentification se fait maintenant directement via l'app mobile
// Le service Supabase n'est plus utilisé pour l'auth côté backend

export {};
